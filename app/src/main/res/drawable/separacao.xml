<shape
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle" >

    // A cor do background
    <solid android:color="@color/cinzaSecundario"
        android:alpha=".20"/>

    // Borda (tamanho e cor)
    <stroke android:width="1dip" android:color="@color/cinzaSecundario" />

    // Arredondamento das bordas

</shape>